#!/usr/bin/env python3

# get user iput for hostname
hostname = input("What value should we set for hostname?")

# check to see if hostname is MTG
if hostname == "MTG":
    # report
    print("The hostname was found to be MTG.")
